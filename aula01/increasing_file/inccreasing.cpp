#include <bits/stdc++.h>
using namespace std;


int main() 
{
    int testcases;
    
    for(int i = 0; i < testcases; i++)
    {
        bool equal = false;
        int len_numbers;
        cin >> len_numbers;
        int numbers[len_numbers];
        for(int j = 0; i < len_numbers; i++)
        {
        cin >> numbers;
        }   

        int not_equal;

        for(int k; k < len_numbers; k++)
        
        {
            not_equal = numbers[k]
            if(numbers[k] == numbers[k+1]) 
            {
                equal = 
            }
        }
       
    }
}