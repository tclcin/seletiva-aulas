#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int gaussian_sum = (n * (n + 1)) / 2;

    if(gaussian_sum % 2 == 0)
    {
        cout << "YES" << endl;
        
        int set_1[n / 2];
        int set_2[n / 2];

        for(int i = n / 2; i > 0; i-- )
        {
            set_1[i] = 
        }


    }


 return 0;   
}

